# ─── PagoMedios MCP Server ─────────────────────────────────────────────────────
# Copia este archivo como .env y completa los valores REALES antes de usar.
#   cp .env.example .env && chmod 600 .env
# ─────────────────────────────────────────────────────────────────────────────

# Bearer Token de integración entregado por Abitmedia / PagoMedios.
PAGOMEDIOS_BEARER_TOKEN=

# (Opcional) URL base de la API.
# PAGOMEDIOS_BASE_URL=https://api.abitmedia.cloud/pagomedios/v2

# (Opcional) Timeout HTTP en segundos.
# PAGOMEDIOS_HTTP_TIMEOUT=30

# Opciones: sse | http_stream
MCP_TRANSPORT_MODE=sse
MCP_PORT=8000
